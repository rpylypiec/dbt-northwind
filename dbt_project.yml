name: 'meu_projeto_dbt'
version: '1.0.0'
config-version: 2

profile: 'meu_projeto_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  meu_projeto_dbt:
    raw:
      enabled: true
      materialized: view

    staging:
      enabled: true
      materialized: view

    intermediate:
      enabled: true
      materialized: view

    marts:
      enabled: true
      schema:  gold
      materialized: table

